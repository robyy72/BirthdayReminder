namespace WebsiteAdmin;

// Note: TicketSource enum is in Common.CommonEnums.cs because it's used by the Ticket model
// which is shared across projects. Only WebsiteAdmin-specific enums should be placed here.
