<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resx="clr-namespace:MobileLanguages;assembly=MobileLanguages"
             x:Class="Mobile.NewBirthdayPage"
             Title="{x:Static resx:Resources.Page_NewBirthday_Title}">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20">

            <!-- Name -->
            <Border Style="{StaticResource Tile}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{x:Static resx:Resources.General_Label_Name}"
                           Style="{StaticResource LabelSectionHeader}" />
                    <Entry x:Name="FirstNameEntry"
                           Placeholder="{x:Static resx:Resources.General_Label_FirstName}"
                           Style="{StaticResource EntryStandard}" />
                    <Entry x:Name="LastNameEntry"
                           Placeholder="{x:Static resx:Resources.General_Label_LastName}"
                           Style="{StaticResource EntryStandard}" />
                </VerticalStackLayout>
            </Border>

            <!-- Birthday -->
            <Border Style="{StaticResource Tile}">
                <VerticalStackLayout Spacing="15">
                    <Label Text="{x:Static resx:Resources.General_Label_Birthday}"
                           Style="{StaticResource LabelSectionHeader}" />

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                        <!-- Day -->
                        <VerticalStackLayout>
                            <Label Text="{x:Static resx:Resources.Date_Day}"
                                   Style="{StaticResource LabelTimeCaption}" />
                            <Picker x:Name="DayPicker"
                                    Style="{StaticResource PickerDayAndMonth}" />
                        </VerticalStackLayout>

                        <!-- Month -->
                        <VerticalStackLayout>
                            <Label Text="{x:Static resx:Resources.Date_Month}"
                                   Style="{StaticResource LabelTimeCaption}" />
                            <Picker x:Name="MonthPicker"
                                    Style="{StaticResource PickerDayAndMonth}" />
                        </VerticalStackLayout>

                        <!-- Year -->
                        <VerticalStackLayout>
                            <Label Text="{x:Static resx:Resources.Date_Year}"
                                   Style="{StaticResource LabelTimeCaption}" />
                            <Picker x:Name="YearPicker"
                                    Style="{StaticResource PickerYear}" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Reminder -->
            <Border Style="{StaticResource Tile}">
                <VerticalStackLayout Spacing="15">
                    <Label Text="{x:Static resx:Resources.Reminder_Label}"
                           Style="{StaticResource LabelSectionHeader}" />

                    <Picker x:Name="ReminderTypePicker"
                            SelectedIndexChanged="OnReminderTypeChanged" />

                    <VerticalStackLayout x:Name="ReminderMethodContainer" IsVisible="False" Spacing="10">
                        <Label Text="{x:Static resx:Resources.ReminderMethod_Label}"
                               Style="{StaticResource LabelBody}" />
                        <Picker x:Name="ReminderMethodPicker" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Save Button -->
            <Button Text="{x:Static resx:Resources.General_Button_Save}"
                    Style="{StaticResource ButtonPrimary}"
                    Clicked="OnSaveClicked" />

            <!-- Delete Button -->
            <Button x:Name="DeleteButton"
                    Text="{x:Static resx:Resources.General_Button_Delete}"
                    Style="{StaticResource ButtonSecondary}"
                    BackgroundColor="#D32F2F"
                    Clicked="OnDeleteClicked"
                    IsVisible="False" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
