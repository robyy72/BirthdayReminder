<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resx="clr-namespace:MobileLanguages;assembly=MobileLanguages"
             x:Class="Mobile.SettingsReminderPage"
             Title="{x:Static resx:Resources.Settings_ReminderTime}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Email Reminder Time -->
            <Label Text="{x:Static resx:Resources.ReminderMethod_Email}"
                   Style="{StaticResource LabelHeader}" />

            <Border Style="{StaticResource Tile}">
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Hours}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="EmailHoursPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>

                    <Label Text=":"
                           Style="{StaticResource LabelTimeSeparator}" />

                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Minutes}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="EmailMinutesPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <!-- SMS Reminder Time -->
            <Label Text="{x:Static resx:Resources.ReminderMethod_Sms}"
                   Style="{StaticResource LabelHeader}" />

            <Border Style="{StaticResource Tile}">
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Hours}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="SmsHoursPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>

                    <Label Text=":"
                           Style="{StaticResource LabelTimeSeparator}" />

                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Minutes}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="SmsMinutesPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <!-- Lock Screen Reminder Time -->
            <Label Text="{x:Static resx:Resources.ReminderMethod_LockScreen}"
                   Style="{StaticResource LabelHeader}" />

            <Border Style="{StaticResource Tile}">
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Hours}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="LockScreenHoursPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>

                    <Label Text=":"
                           Style="{StaticResource LabelTimeSeparator}" />

                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Minutes}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="LockScreenMinutesPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <!-- WhatsApp Reminder Time -->
            <Label Text="{x:Static resx:Resources.ReminderMethod_WhatsApp}"
                   Style="{StaticResource LabelHeader}" />

            <Border Style="{StaticResource Tile}">
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Hours}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="WhatsAppHoursPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>

                    <Label Text=":"
                           Style="{StaticResource LabelTimeSeparator}" />

                    <VerticalStackLayout>
                        <Label Text="{x:Static resx:Resources.Time_Minutes}"
                               Style="{StaticResource LabelTimeCaption}" />
                        <Picker x:Name="WhatsAppMinutesPicker"
                                Style="{StaticResource PickerTime}" />
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Border>

            <!-- Remind Until Approved -->
            <Border Style="{StaticResource Tile}">
                <Grid ColumnDefinitions="*,Auto">
                    <Label Grid.Column="0"
                           Text="{x:Static resx:Resources.RemindUntilApproved_Label}"
                           Style="{StaticResource LabelBody}"
                           VerticalOptions="Center" />
                    <Switch x:Name="RemindUntilApprovedSwitch"
                            Grid.Column="1"
                            VerticalOptions="Center" />
                </Grid>
            </Border>

            <!-- Save Button -->
            <Button Text="{x:Static resx:Resources.General_Button_Save}"
                    Style="{StaticResource ButtonPrimary}"
                    Clicked="OnSaveClicked" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
